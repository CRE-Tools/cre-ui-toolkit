{
  "name": "cre-ui-toolkit",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @cre/cre-web-ui build",
    "storybook": "pnpm --filter @cre/storybook dev",
    "sandbox": "pnpm --filter @cre/sandbox dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}